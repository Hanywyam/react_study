import React from "react";

const TodosInput = () => {
  return (
    <form>
      <input type="text" placeholder="할 일을 입력하세요." />
      <button>확인</button>
    </form>
  );
};

export default TodosInput;
