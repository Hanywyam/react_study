import React from "react";

const Research3 = ({ txtValue }) => {
  return (
    <div>
      <li>{txtValue}</li>
      <button>이전</button>
      <button>다음</button>
    </div>
  );
};

export default Research3;
